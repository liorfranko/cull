# Baseline (no skill) — Eval 3

Agent attempted to fetch the remote file via Bash/WebFetch/GitHub MCP but all were denied. It had no structured flow for resolving a short name from sources.json, no adaptation strategy framework, and no upstream.yaml schema to follow.
